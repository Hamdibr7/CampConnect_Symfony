{% extends 'base_back.html.twig' %}

{% block title %}Modifier l'Équipement{% endblock %}

{% block css %}
    {{ parent() }}
    <style>
        .pcoded-content {
            margin-left: 200px;
            padding: 30px;
            width: calc(100% - 200px);
        }

        .card {
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.1);
            padding: 25px;
            margin-top: 20px;
            width: 100%;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            margin-bottom: 5px;
            font-weight: 500;
        }

        .form-control {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            transition: border-color 0.3s ease;
        }

        .form-control:focus {
            border-color: #03A9F4;
            outline: none;
            box-shadow: 0 0 0 2px rgba(3, 169, 244, 0.2);
        }

        .form-control.is-invalid {
            border-color: #dc3545;
        }

        textarea.form-control {
            min-height: 100px;
            resize: vertical;
        }

        .btn-submit {
            background-color: #03A9F4;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn-submit:hover {
            background-color: #0288D1;
        }

        .btn-cancel {
            background-color: #6c757d;
            color: white;
            padding: 10px 20px;
            border-radius: 4px;
            text-decoration: none;
            margin-left: 10px;
            transition: background-color 0.3s ease;
            display: inline-block;
        }

        .btn-cancel:hover {
            background-color: #5a6268;
            color: white;
        }

        .current-image {
            max-width: 200px;
            max-height: 150px;
            margin-top: 10px;
            border-radius: 4px;
            border: 1px solid #ddd;
            padding: 5px;
            display: block;
        }

        .image-preview {
            max-width: 200px;
            max-height: 150px;
            margin-top: 10px;
            display: none;
            border-radius: 4px;
            border: 1px solid #ddd;
            padding: 5px;
        }

        .required-field::after {
            content: " *";
            color: red;
        }

        .form-error {
            color: #dc3545;
            font-size: 0.875em;
            margin-top: 5px;
            display: block;
        }

        h2 {
            color: #333;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #eee;
        }

        .fa {
            margin-right: 8px;
        }

        .text-muted {
            color: #6c757d;
            font-style: italic;
        }
    </style>
{% endblock %}

{% block body %}
<div class="pcoded-content">
    <div class="card">
        <h2><i class="fas fa-edit"></i> Modifier l'Équipement</h2>

        {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'needs-validation'}}) }}

            <div class="form-group">
                {{ form_label(form.nomEquip, "Nom de l'équipement", {"label_attr": {"class": "form-label required-field"}}) }}
                {{ form_widget(form.nomEquip, {"attr": {"class": "form-control"}}) }}
                {{ form_errors(form.nomEquip) }}
            </div>

            <div class="form-group">
                {{ form_label(form.qte_dispo, "Quantité disponible", {"label_attr": {"class": "form-label required-field"}}) }}
                {{ form_widget(form.qte_dispo, {"attr": {"class": "form-control"}}) }}
                {{ form_errors(form.qte_dispo) }}
            </div>

            <div class="form-group">
                {{ form_label(form.prix, "Prix (€)", {"label_attr": {"class": "form-label required-field"}}) }}
                {{ form_widget(form.prix, {"attr": {"class": "form-control"}}) }}
                {{ form_errors(form.prix) }}
            </div>

            <div class="form-group">
                {{ form_label(form.imageFile, "Nouvelle Image", {"label_attr": {"class": "form-label"}}) }}
                {{ form_widget(form.imageFile, {"attr": {"class": "form-control", "onchange": "previewImage(this)"}}) }}

                {% if equipement.image %}
                    <p>Image actuelle :</p>
                    <img src="{{ asset('uploads/equipements/' ~ equipement.image) }}" class="current-image" alt="Image actuelle">
                    <small class="text-muted">Laissez vide pour conserver l'image actuelle</small>
                {% else %}
                    <p class="text-muted">Aucune image actuelle</p>
                {% endif %}

                <img id="imagePreview" class="image-preview" alt="Aperçu de la nouvelle image">
                {{ form_errors(form.imageFile) }}
            </div>

            <div class="form-group">
                {{ form_label(form.description, "Description", {"label_attr": {"class": "form-label"}}) }}
                {{ form_widget(form.description, {"attr": {"class": "form-control"}}) }}
                {{ form_errors(form.description) }}
            </div>

            <div class="form-group">
                <button type="submit" class="btn-submit">
                    <i class="fas fa-save"></i> Enregistrer les modifications
                </button>
                <a href="{{ path('app_equipement_index') }}" class="btn-cancel">
                    <i class="fas fa-times"></i> Annuler
                </a>
            </div>
        {{ form_end(form) }}
    </div>
</div>

<script>
    function previewImage(input) {
        const preview = document.getElementById('imagePreview');
        const file = input.files[0];
        const reader = new FileReader();

        if (file) {
            reader.onload = function(e) {
                preview.src = e.target.result;
                preview.style.display = 'block';
            };
            reader.readAsDataURL(file);
        } else {
            preview.style.display = 'none';
            preview.src = '';
        }
    }

    // Initialize the preview if there's already an image selected
    document.addEventListener('DOMContentLoaded', function() {
        const fileInput = document.querySelector('input[type="file"]');
        if (fileInput && fileInput.files.length > 0) {
            previewImage(fileInput);
        }
    });
</script>
{% endblock %}
