<h2>Mes Notifications</h2>

{% if notifications is empty %}
    <p>Aucune notification pour le moment.</p>
{% else %}
    <ul>
        {% for notif in notifications %}
            <li>
                {{ notif.message }} - <small>{{ notif.date|date('d/m/Y H:i') }}</small>
            </li>
        {% endfor %}
    </ul>
{% endif %}
